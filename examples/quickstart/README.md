# Quickstart Example

This folder shows a minimal setup.

1. Copy `templates/minimal.yaml` to your project as `ragkit.yaml`.
2. Place documents in `data/documents`.
3. Run:

```bash
ragkit ingest
ragkit serve
```
